{
  "title": "RFW",
  "type": "object",
  "contains:": {
    "rfw_id:": {
      "type": "integer"
    },
    "benchmark_type:": {
      "type": "integer"
    },
    "workload_metric:": {
      "description": "Workload Metric (such as CPU or NetworkIn or NetworkOut or Memory)",
      "type": "integer",
      "min": "1",
      "max": "4"
    },
    "batch_unit:": {
      "type": "integer"
    },
    "batch_id:": {
      "type": "integer"
    },
    "batch_size:": {
      "type": "integer"
    },
    "batch_type:": {
      "type": "integer"
    },
    "data_analytics": {
      "type": "string"
    }
  },
  "required": [
    "rfw_id",
    "benchmark_type",
    "workload_metric",
    "batch_unit",
    "batch_id",
    "batch_size",
    "batch_type",
    "data_analytics"
  ]
}